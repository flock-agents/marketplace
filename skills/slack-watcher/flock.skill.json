{
  "name": "Slack Watcher",
  "slug": "slack-watcher",
  "icon": "icon.svg",
  "description": "Watch Slack channels for actionable items and create Flock tasks",
  "version": "1.1.0",
  "author": "flock",
  "category": "productivity",
  "tags": ["slack", "tasks", "automation", "monitoring"],
  "minFlockVersion": "0.7.0",
  "tier": "installable",
  "nature": "agentic",
  "connectorDeps": [
    { "connectorId": "slack", "required": true }
  ],
  "skillDeps": [],
  "routines": [
    {
      "id": "watch-channels",
      "name": "Watch Slack channels",
      "trigger": { "connectorId": "slack", "eventId": "slack_message" },
      "eventKinds": ["slack_message", "slack_mention"],
      "executionMode": "llm",
      "prompt": "New Slack activity detected. Review the messages, identify actionable items (tasks, follow-ups, reminders, learnings), and create Flock tasks for each one.",
      "configSchema": [
        {
          "key": "channels",
          "type": "multi-select",
          "label": "Channels to watch",
          "help": "Select which Slack channels to monitor",
          "options": { "kind": "dynamic", "from": "listChannels" }
        },
        {
          "key": "ignoreBots",
          "type": "boolean",
          "label": "Ignore bot messages",
          "default": true
        },
        {
          "key": "keywords",
          "type": "text",
          "label": "Keyword filter (comma-separated)",
          "help": "Only surface messages containing these keywords. Leave blank for all."
        }
      ],
      "pollingCadence": {
        "recommended": 86400000,
        "min": 3600000,
        "max": 604800000,
        "userConfigurable": true
      }
    }
  ]
}
